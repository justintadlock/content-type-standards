<?php

add_action( 'init', 'prefix_events_register_meta' );

function prefix_events_register_meta() {

	/* Register post meta. */

	// register_meta( 'post', $meta_key, $sanitization_callback, $auth_callback );
}
